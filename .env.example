# Copy to .env:  cp .env.example .env

# ⚠️  Change in production — generate with:
# node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=enn_change_this_secret_in_production

# Port (default 5174)
PORT=5174
